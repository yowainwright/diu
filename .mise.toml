[tools]
go = "1.25"
golangci-lint = "latest"
goreleaser = "latest"

[env]
CGO_ENABLED = "0"
GOFLAGS = "-mod=readonly"

[tasks.build]
description = "Build the DIU binary"
run = "go build -o diu ./cmd/diu"

[tasks.test]
description = "Run unit and integration tests"
run = "go test -v -race -coverprofile=coverage.out ./..."

[tasks.test-unit]
description = "Run unit tests only"
run = "go test -v -short ./cmd/... ./internal/... ./pkg/..."

[tasks.test-integration]
description = "Run integration tests"
run = "go test -v ./integration/..."

[tasks.test-e2e]
description = "Run Docker E2E tests"
run = "docker compose run --build --rm e2e"

[tasks.test-coverage]
description = "Run tests with coverage report"
run = """
go test -v -race -coverprofile=coverage.out ./...
go tool cover -html=coverage.out -o coverage.html
open coverage.html
"""

[tasks.lint]
description = "Run linters"
run = """
go vet ./...
golangci-lint run ./...
"""

[tasks.fmt]
description = "Format code"
run = "gofmt -w ."

[tasks.check]
description = "Run all checks (format, lint, test)"
depends = ["fmt", "lint", "test"]

[tasks.clean]
description = "Clean build artifacts"
run = """
go clean
rm -rf diu coverage.out coverage.html dist/
"""

[tasks.install]
description = "Install DIU locally"
depends = ["build"]
run = "cp diu /usr/local/bin/"

[tasks.uninstall]
description = "Uninstall DIU"
run = "rm -f /usr/local/bin/diu"

# Development tasks
[tasks.dev]
description = "Run in development mode with auto-reload"
run = """
if ! command -v air &> /dev/null; then
  go install github.com/cosmtrek/air@latest
fi
air
"""

[tasks.run]
description = "Build and run DIU"
depends = ["build"]
run = "./diu"

# Shell hook tasks
[tasks.hooks-setup]
description = "Inject diu shell hooks into .zshrc / .bashrc"
depends = ["build"]
run = "./diu setup"

[tasks.hooks-teardown]
description = "Remove diu shell hooks"
run = "./diu teardown"

[tasks.hooks-status]
description = "Check shell hook status"
run = "./diu status"

[tasks.scan]
description = "Scan installed packages and update cache"
depends = ["build"]
run = "./diu scan"

[tasks.stats]
description = "Show statistics"
run = "./diu stats --top 10"

[tasks.unused]
description = "Show packages unused for 6+ months"
run = "./diu list --unused 6m"

# Release tasks
[tasks.release-snapshot]
description = "Create a release snapshot"
run = "goreleaser release --snapshot --clean"

[tasks.release-check]
description = "Check if ready for release"
run = "goreleaser check"

[tasks.release]
description = "Create a new release"
run = "goreleaser release --clean"

# Docker tasks
[tasks.docker-build]
description = "Build Docker image"
run = "docker build -t diu:latest ."

[tasks.docker-e2e]
description = "Run E2E tests in Docker"
run = "docker compose run --build --rm e2e"

# Setup tasks
[tasks.setup]
description = "Initialize project for development"
run = """
go mod download
mkdir -p ~/.config/diu ~/.local/share/diu ~/.local/bin/diu-wrappers
echo "✓ Project initialized"
"""

[tasks.deps]
description = "Download dependencies"
run = "go mod download"

[tasks.deps-update]
description = "Update dependencies"
run = """
go get -u ./...
go mod tidy
"""

[tasks.deps-tidy]
description = "Tidy dependencies"
run = "go mod tidy"

# CI simulation
[tasks.ci]
description = "Run CI pipeline locally"
depends = ["clean", "deps", "lint", "test", "build"]
